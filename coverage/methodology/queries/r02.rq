PREFIX vcfc: <https://w3id.org/vcf-core/vocab#>
# Expected answers are authored in inputs/cases.json from the source VCF.
SELECT ?i ?key ?value WHERE { ?h vcfc:hasHeaderLine ?line . ?line vcfc:lineIndex ?i ; vcfc:headerKey ?key ; vcfc:headerValue ?value . FILTER(?i <= 3) }
