PREFIX vcfc: <https://w3id.org/vcf-core/vocab#>
# Expected answers are authored in inputs/cases.json from the source VCF.
SELECT ?index ?code WHERE { ?r vcfc:pos 30 ; vcfc:hasCall/vcfc:hasFilterCode ?f . ?f vcfc:componentIndex ?index ; vcfc:filterCodeValue ?code }
